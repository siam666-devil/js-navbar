function imposterFormula(i, p) {
	let formula = (100 * (i / p))
	console.log(`${Math.rou(formula)}%`)
}
imposterFormula(3, 10)
imposterFormula(2, 4)
imposterFormula(1, 3)
imposterFormula(2, 7)
imposterFormula(3, 8)
